appname: go-blog-on-lain
build:
  base: kaizhang91/go-blog-on-docker
  script:
    - echo "building..."
release:
  dest_base: kaizhang91/go-blog-on-docker
  copy:
    - src: /lain/app/lain.yaml
      dest: /lain/app/lain.yaml
web:
  num_instances: 1
  # 不写 cmd -> None
  # cmd:    # 空格 -> None
  # cmd: 1
  # cmd: /lain/app/blog/blog
  # cmd: /lain/app/blog/blog -http=:8080
  # cmd: []
  # cmd: ["/lain/app/blog/blog"]
  # cmd: ["-http=:8080"]
  # cmd: ["/lain/app/blog/blog", "-http", ":8080"]
  # cmd: ["/lain/app/blog/blog", "-http", 8080]
  # cmd: ["/lain/app/blog/blog",     "-http=:8080"]
  # cmd: /lain/app/blog/blog       -http=:8080
  port: 8080
  logs:
    - monitor.log
