appname: go-blog-on-lain

build:
  base: kaizhang91/go-blog-on-docker
  prepare:
    version: 4
    script:
      - echo "preparing..."
  script:
    - echo "building..."

web:
  # entrypoint: blog
  entrypoint: blog -http=:8080
  # entrypoint: blog -http=:8080 -content=content/
  # entrypoint: ["blog"]
  # entrypoint: ["blog", "-http=:8080"]
  # entrypoint: ["blog", "-http=:8080", "-content=content/"]
  # 不写 cmd -> None
  # cmd:    # 空格 -> None
  # cmd: 1
  # cmd: -http=:8080
  # cmd: -http=:8080 -content=content/
  # cmd: []
  # cmd: ["-http=:8080"]
  # cmd: ["-http=:8080", "content=content/"]
  port: 8080
  logs:
    - monitor.log
