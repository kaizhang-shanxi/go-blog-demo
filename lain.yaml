appname: go-blog-on-lain

build:
  base: kaizhang91/go-blog-on-docker
  prepare:
    version: 0
    script:
      - echo "preparing..."
  script:
    - echo "building..."

release:
  dest_base: kaizhang91/go-blog-on-docker
  copy:
    - src: /lain/app/README.md
      dest: /lain/app/README.md

web:
  num_instances: 1
  # 不写 cmd -> None
  # cmd:    # 空格 -> None
  # cmd: 1
  # cmd: /lain/app/blog/blog
  # cmd: /lain/app/blog/blog -http=:8080
  # cmd: []
  # cmd: ["/lain/app/blog/blog"]
  # cmd: ["-http=:8080"]
  # cmd: ["/lain/app/blog/blog", "-http", ":8080"]
  # cmd: ["/lain/app/blog/blog", "-http", 8080]
  # cmd: ["/lain/app/blog/blog",     "-http=:8080"]
  # cmd: /lain/app/blog/blog       -http=:8080
  port: 8080
  logs:
    - monitor.log
