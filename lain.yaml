appname: go-blog-on-lain

build:
  # base: registry.yxapp.xyz/centos-golang:go1.6.2
  base: kaizhang91/go-blog-on-docker
  prepare:
    version: 1
    script:
      - echo "preparing..."
  script:
    - echo "building..."

web:
  image: kaizhang91/go-blog-on-docker
  # 不写 cmd -> None
  # cmd:    # 空格 -> None
  # cmd: 1
  # cmd: /lain/app/blog/blog
  # cmd: /lain/app/blog/blog -http=:8080
  # cmd: []
  # cmd: ["/lain/app/blog/blog"]
  # cmd: ["/lain/app/blog/blog", "-http=:8080"]
  # cmd: ["/lain/app/blog/blog", "-http", ":8080"]
  # cmd: ["/lain/app/blog/blog", "-http", 8080]
  # cmd: ["/lain/app/blog/blog",     "-http=:8080"]
  # cmd: /lain/app/blog/blog       -http=:8080
  port: 8080
  logs:
    - monitor.log
